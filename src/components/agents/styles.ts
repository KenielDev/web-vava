/* eslint-disable import/first */
import styled from "styled-components";

export const ButtonTitleAgent = styled.button`
  width: 100%;
  font-size: 120px;
  line-height: 103px;
  font-weight: 400;
  text-align: left;
  transition-duration: 300ms;

  &:hover {
    transform: translateX(0.5rem);
  }
`;
