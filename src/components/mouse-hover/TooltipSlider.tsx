import React from "react";
import { WrapperTooltip } from "./styles";

export interface TooltipProps {
  x: number;
  y: number;
  children?: React.ReactNode;
}

export function TooltipSlider({ x, y }: TooltipProps) {
  return <></>;
}
